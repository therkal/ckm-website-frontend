import { LazyLoadImagesDirectiveDirective } from './lazy-load-images-directive.directive';

describe('LazyLoadImagesDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new LazyLoadImagesDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
