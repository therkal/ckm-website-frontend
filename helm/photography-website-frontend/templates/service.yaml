apiVersion: v1
kind: Service
metadata:
  name: {{ include "photography-website-frontend.fullname" . }}
  labels:
    {{- include "photography-website-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "photography-website-frontend.selectorLabels" . | nindent 4 }}
